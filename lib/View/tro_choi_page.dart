import 'package:flutter/material.dart';

class TroChoiPage extends StatelessWidget {
  const TroChoiPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Tro choi page'),
    );
  }
}
